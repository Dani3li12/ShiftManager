import styled from "styled-components";


export const Title = styled.div`
    color: var(--blue);
    font-weight: bold;
    font-size: 1.8rem;
    text-align: center;
    padding-bottom: 2.8rem;
}
`